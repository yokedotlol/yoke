{
  "name": "yoke-og-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/worker.js --target=browser --minify --loader:.txt=text --external=*.wasm && cp src/resvg_bg.wasm dist/",
    "typecheck": "bun x tsc --noEmit",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250516.0",
    "typescript": "5.6.3",
    "wrangler": "^3.114.0"
  }
}
