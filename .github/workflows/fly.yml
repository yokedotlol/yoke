name: Deploy Fly Proxy

on:
  push:
    branches: [main]
    paths:
      - 'fly-proxy/**'
      - '.github/workflows/fly.yml'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual deploy (e.g. GeoLite2 update, config change)'
        required: false
        type: string

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Run Go tests
        working-directory: fly-proxy
        run: go test -v ./...

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    steps:
      - uses: actions/checkout@v5

      - name: Setup Fly CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy (blue-green)
        working-directory: fly-proxy
        run: flyctl deploy --remote-only --strategy bluegreen --build-arg "MAXMIND_LICENSE_KEY=$MAXMIND_LICENSE_KEY"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          MAXMIND_LICENSE_KEY: ${{ secrets.MAXMIND_LICENSE_KEY }}

  smoke-test:
    needs: deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    steps:
      - name: Wait for traffic routing
        run: sleep 15

      - name: Smoke test — Health endpoint (multi-region)
        run: |
          echo "Checking /health..."
          RESP=$(curl -sf --max-time 10 https://yoke-probe.fly.dev/health)
          echo "$RESP" | jq -e '.status == "ok"'
          echo "✓ Health OK ($(echo "$RESP" | jq -r '.health'))"

          echo ""
          echo "Verifying machines are running in expected regions..."
          # Fly routes to nearest — just confirm global endpoint responds
          for i in 1 2 3; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 https://yoke-probe.fly.dev/health)
            echo "  Request $i: HTTP $STATUS"
            if [ "$STATUS" != "200" ]; then
              echo "::warning::Health check returned $STATUS on attempt $i"
            fi
          done

      # Exercise real proxy paths against big domains (3/5 must pass)
      - name: Smoke test — Real domain probes
        run: |
          DOMAINS=("google.com" "apple.com" "amazon.com" "microsoft.com" "cloudflare.com")
          PASS=0
          FAIL=0
          AUTH_SECRET="${{ secrets.FLY_AUTH_SECRET }}"

          for domain in "${DOMAINS[@]}"; do
            echo -n "  probe-status $domain... "
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 \
              -H "Authorization: Bearer ${AUTH_SECRET}" \
              "https://yoke-probe.fly.dev/probe-status?domain=${domain}")
            if [ "$STATUS" -ge 200 ] && [ "$STATUS" -lt 500 ]; then
              echo "✓ ($STATUS)"
              PASS=$((PASS + 1))
            else
              echo "✗ ($STATUS)"
              FAIL=$((FAIL + 1))
            fi
          done

          echo ""
          echo "Results: ${PASS}/5 passed, ${FAIL}/5 failed"

          if [ "$PASS" -lt 3 ]; then
            echo "::error::Only ${PASS}/5 domain probes passed — proxy may be broken"
            exit 1
          else
            echo "✓ Smoke tests passed (${PASS}/5 — threshold is 3/5)"
          fi

      - name: 🚨 Alert on failure
        if: failure()
        run: |
          echo "::error::Fly proxy smoke tests failed after blue-green deploy!"
          echo "::error::Fly health checks should have caught this — check deploy logs"
          echo "::error::Dashboard: https://fly.io/apps/yoke-probe"
          exit 1
